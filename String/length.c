#include <stdio.h>
#include <string.h>
 
int main( )
{
    int len;
    char array[20]="payilagam.com" ;
 
    len = strlen(array) ;
 
    printf ( "\n string length  = %d \n" , len ) ;
    return 0;
}